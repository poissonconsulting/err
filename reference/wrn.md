# Warning

Immediately outputs an warning without the call as part of the error
message that as far as possible is a single line.

## Usage

``` r
wrn(...)
```

## Arguments

- ...:

  zero or more objects which can be coerced to character

## See also

[`warning`](https://rdrr.io/r/base/warning.html),
[`err`](https://poissonconsulting.github.io/err/reference/err.md),
[`msg`](https://poissonconsulting.github.io/err/reference/msg.md) and
[`co`](https://poissonconsulting.github.io/err/reference/co.md)
